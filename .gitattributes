# Normalize text to LF in the repository.
* text=auto eol=lf

# Fixture files must keep their exact bytes (CRLF, UTF-16, BOM, partial records).
fixtures/** -text
crates/*/tests/fixtures/** -text

# Binary assets
*.png binary
*.ico binary
*.icns binary
*.parquet binary
