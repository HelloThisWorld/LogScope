{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the v0.0 proof window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
