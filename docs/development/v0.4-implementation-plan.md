# LogScope v0.4 Implementation Plan — Living Status Document

Updated: 2026-08-05 (preflight re-run after v0.3 completion)
Branch: `feature/v0.4-wp0-plan` (from `main` @ `47dc42b`)

v0.4 goal: deterministic, explainable log analysis on top of the completed
v0.3 investigation and evidence workflow — message patterns and stack-trace
fingerprints, baseline-versus-suspect comparison, bounded exact /
identifier / temporal correlation, deterministic findings, and analysis
evidence flowing into reports and case bundles. No LLM, no opaque scores:
every result carries its rule, inputs, formula, and limitations.

**Status: the 2026-07-31 preflight BLOCKED verdict is cleared. v0.3 is
complete (`main` @ `47dc42b`, matrix 29 PASS / 1 PARTIAL in
`v0.3-implementation-plan.md`), and the re-run below records every v0.4
prerequisite as PASS. WP1 (versioned analysis domain) is unblocked; this
session's deliverable is the re-run plus the design pass the first
preflight deliberately deferred.**

## Inspected baseline (re-run, 2026-08-05)

- `main` @ `47dc42b` = v0.3 complete at version 0.3.0 (PR #25). The
  "actual v0.3 acceptance report" gate 1 requires now exists: the final
  32-gate matrix in `docs/development/v0.3-implementation-plan.md`
  (29 PASS, 1 PARTIAL — gate 18's mid-generation cancellation clause;
  interactive-walkthrough owner residual on gates 6/15/29/30).
- Versions everywhere 0.3.0 (workspace `Cargo.toml`, `package.json`,
  `tauri.conf.json`); `CHANGELOG.md` has the 0.3.0 entry. No tag or
  GitHub Release for 0.3.0 (owner decision pending) — the artifacts and
  checksums are recorded in the v0.3 release record.
- Schema lineage: v1 `0001_init` → v2 `0002_explorer` → v3
  `0003_investigations` (`supported_schema_version()` = 3). v0.4 adds
  `0004_analysis`.
- Contract versions: evidence envelope v1, query language v1, Parquet
  storage schema v1, workspace schema v3, bundle schema v1
  (`min_compatible_version` gate proven), profile contract v1.
- Suite: **292 tests / 0 failures** (55 suites) on `main`; fmt clean,
  clippy `-D warnings` clean; `tsc && vite build` clean. Stable
  error-code registry (120 codes) enforced by source scan in
  `crates/logscope-app/tests/error_codes.rs` — every new v0.4 code must
  be registered there.
- CI: hosted 3-platform matrix (Linux fmt+clippy+tests / Windows
  tests+shell smoke / macOS shared-core), warm 1–4 min, green on every
  recent push (latest: PR #25 run 31009362944).
- Packaging: `scripts/package-portable.ps1` emits BOTH artifacts from one
  payload with construction-time byte-equivalence via
  `tools/logscope-setup` (0.3.0 run recorded; portable exe
  smoke-launched).
- No earlier analysis code exists to reuse: verified 2026-07-31 (no
  template/fingerprint/comparison/correlation modules anywhere) and
  nothing in W3–W-R added any. `logscope-query/src/spans.rs` is a
  v0.8-relevant spike (in-memory trace reconstruction) and is out of
  scope here (trace/span display is forbidden in v0.4; trace/span IDs on
  log records are correlation keys only).
- Analysis-relevant canonical surface (verified in `logscope-model`):
  typed body/display message, typed dynamic attributes, Resource/Scope
  descriptors, generic operation/outcome/event fields, trace/span IDs
  where the profile mapped them, timestamp-quality flags,
  `IngestProvenance` (raw hash, locator, parser/profile versions),
  deterministic `log-<32hex>` content IDs, `dsrev-<hex>` dataset
  revisions.

## Prerequisite acceptance matrix (mandatory preflight — re-run 2026-08-05)

Machine: i9-12900KF, 64 GB, NVMe, Windows 11 Pro 10.0.26100; toolchain
rustc/cargo 1.97.1, node 22.6. Evidence cites the v0.3 final matrix and
this week's measured runs rather than re-executing unchanged benchmarks
(per the milestone's own rule).

| # | Prerequisite | Status | Evidence |
|---|---|---|---|
| P1 | Desktop app launches in current dev environment | PASS | packaged **0.3.0** exe extracted from the portable ZIP and smoke-launched 2026-08-05: window `LogScope`, WS 47 MiB, clean termination; `tsc && vite build` 516 ms |
| P2 | Real v0.3 workspace create/open/close/reopen/migrate | PASS | 292-suite sweep green on `main`: `lifecycle`, `pre_v02_workspace_migrates_and_stays_searchable`, case_domain v2→v3 fixture, `e2e_workflow` reopen legs |
| P3 | Built-in Import Profile completes a real log-only import | PASS | `explorer_flow` (ES profile), `e2e_workflow` (240-record import through the job path), fixture goldens |
| P4 | Explorer free-text + structured queries via authoritative service | PASS | `explorer_service` (14), language conformance + property suites |
| P5 | Results/histograms/facets/summaries/saved views/**evidence/reports**/exports retain equivalent query+time semantics | PASS (was FAIL) | shared compiled semantics suite green; evidence pins run through `prepare_scope` (same analyze/compile pipeline, frozen concrete bounds); report snapshots capture evidence/marker revisions and render deterministically (`report_proofs`, preview==bytes in `e2e_workflow`) |
| P6 | Canonical events: stable IDs survive close/reopen | PASS | deterministic `log-<32hex>` (`determinism` suite); reopen suites |
| P7 | Typed body/attrs/Resource/Scope/generic fields/ts-quality/provenance retained | PASS | `RecordDetail` completeness test green |
| P8 | Dataset revisions + immutable segments durable; can invalidate derived analysis safely | PASS | `dsrev-<hex>` over the published segment set; determinism + append-drift proofs in `case_flow`; segments immutable by construction |
| P9 | Query definitions preserve text/version/fingerprint/selection/bounds/filters | PASS | envelope `QueryContext` (text, language_version, `qry-` fingerprint, dataset_ids, strategy JSON, resolved half-open bounds, omitted_untimestamped); RelativeToLatest freezes to concrete bounds at pin time (proven) |
| P10 | Investigation model distinguishes user-authored from deterministic findings | PASS | `investigation_items.authored_by_user` (=1 for all v0.3 rows) + reserved `finding_provenance_json` (NULL in v0.3) — the exact seam WP5 fills |
| P11 | Evidence separates live refs from snapshots; can accept typed analysis kinds | PASS | separate `reference_json`/`snapshot_json`; version-gated decode refuses newer envelopes (corpus-proven, never panics); analysis kinds arrive as envelope v2 without changing v1 meaning |
| P12 | Report generation captures one revision + shared disclosure projection | PASS (was FAIL) | one snapshot per generation, byte-deterministic, preview==final asserted; ONE `Projection` shared by preview/report/bundle (`redaction_proofs`, ADR-0019) |
| P13 | Bundles represent full/subset/snapshot reproduction honestly | PASS (was FAIL) | `.logscope-case` manifest declares `reproduction_scope`; v0.3 ships `snapshot_only` honestly (imported verification reports `dataset_revision_unavailable`, never `verified` — e2e-proven); hostile-import validation suite green |
| P14 | Long query/evidence/report/export/bundle ops: progress, cancel, atomic publication, cleanup | PASS (was FAIL-partial) | query/verify/export jobs cancellable (measured); report/bundle generation atomic + staged + tombstone-recovered; the one recorded deviation (report/bundle generation is synchronous — 62 ms / 2.9 s measured at benchmark scale) is documented in the v0.3 matrix and does not gate analysis runs, which will use the proven `logscope_jobs` pattern from day one |
| P15 | v0.3 migrations + recovery tests pass | PASS | migration idempotency + SchemaTooNew refusal + lifecycle recovery + W8 tombstone recovery (idempotent reopen) green |
| P16 | Runtime no-outbound-network tests pass | PASS | `offline_probe` + `receiver_hardening` green in the 292 sweep |
| P17 | Graphical extraction package + portable ZIP from one byte-equivalent payload | PASS (was FAIL-inherited) | 0.3.0 release record: both artifacts from one script run, setup embeds the exact ZIP, digest re-read through the stub's own trailer parser and asserted equal; smoke-launched |
| P18 | Platform-neutral core boundaries viable for macOS | PASS | hosted macOS CI leg green on every push; `logscope-case` dependency-free; no host-path parsing in shared code |

Additional baseline facts from the first preflight, now resolved:
- Investigation/evidence UX exists (W3–W4): pin dialogs, evidence panel,
  investigation view, timeline — gate 30's "through real UI flows"
  carries the same interactive-walkthrough residual v0.3 recorded.
- The v0.3 acceptance matrix exists and was inspected (gate 1 evidence).

**Preflight decision: all 18 prerequisites PASS. v0.4 implementation
proceeds at WP1.**

## First preflight (2026-07-31) — historical record

The original preflight ran against the W0–W2-only tree and returned
**BLOCKED** on P5/P12/P13/P14/P17 (no reports, no disclosure projection,
no bundles, no report/bundle operation guarantees, single-artifact
packaging) plus the missing evidence UX and missing v0.3 acceptance
report. No analysis code was written then, per the stop rule: analysis
results must not be built on a baseline that cannot capture, report, or
bundle them. The remediation chain it prescribed (complete v0.3 W3–W-R,
then re-run) is exactly what happened between 2026-08-04 and 2026-08-05
(PRs #18–#25). The full original record is preserved in this file's git
history (`a878a46` + `7437f92`, 2026-07-31); its "modules to reuse"
table is carried forward below unchanged in substance.

## Existing modules and contracts to reuse

| Module | Reuse for v0.4 |
|---|---|
| `logscope-model` | deterministic `log-<32hex>` IDs; `stable_id`/`Digest` (BLAKE3) for content-addressed pattern/config fingerprints; `IngestProvenance`, `QualityFlags` |
| `logscope-workspace` | forward-only migrations (next: `0004_analysis`); STRICT tables; repo-per-domain pattern (`case_meta.rs` → new `analysis_meta.rs`); staging/publish/recovery; **`MetaDb::raw()` is a non-reentrant mutex — repository methods must never re-enter it** |
| `logscope-query-lang` | language v1 canonical AST JSON + `qry-` fingerprints → the normalized query fingerprint every run records |
| `logscope-query` | sole owner of parsing/validation/planning/bounded execution; `stream_query` for bounded canonical record streams into analysis; `TimeStrategy`/`resolve_window`; cancel registry + F-1-fixed pre-execution cancellation |
| `logscope-jobs` | cancellable jobs with progress → analysis runs and rebuilds (two-phase run records exactly like report artifacts) |
| `logscope-case` | envelope versioning: analysis evidence kinds arrive as envelope v2; v1 decode stays intact (corpus-proven) |
| `logscope-app/src/case.rs` | `dataset_revision` capture; pin-service + batched-resolver patterns; `prepare_scope` as the one way to freeze a query scope |
| `logscope-app/src/{report,redact,bundle}.rs` | render sections, the single disclosure projection, bundle entries + reproduction scopes — WP6 extends these, never forks them |
| `logscope-store` / DuckDB / Parquet | immutable segments as the bulk source; DuckDB as the bounded analytical engine; derived Parquet under the workspace for pattern membership (rebuildable, keyed to canonical IDs) |
| `logscope-testsupport` | deterministic ChaCha8 corpus generators (seed 20260729) — extend with pattern/stack/retry/skew shapes for WP8 fixtures |

## Ordered work packages and live status

| # | Package | Status |
|---|---|---|
| WP0 | Preflight re-run + design pass (this document) | DONE (this branch) |
| WP1 | Versioned analysis domain: migration `0004_analysis`, definitions/runs repositories, deterministic identity, run lifecycle via jobs | DONE (this branch) — schema v4; no algorithm and no UI yet by design (nothing in WP1 can complete a run with fabricated results) |
| WP2 | Message patterns + stack fingerprints + Pattern Explorer | DONE — engine (PR #28, ADR-0020) + Pattern Explorer UX (this branch); standing interactive-walkthrough residual (no UI automation) |
| WP3 | Baseline-versus-suspect comparison | DONE — engine (PR #30) + Compare UX and the canonical generic-field dimensions (this branch); standing interactive-walkthrough residual |
| WP4 | Deterministic correlation (exact/correlated/probable; retry/duplicate/skew/gap) | DONE (ADR-0021) — WP4a exact/correlated grouping (PR #32), WP4b probable neighborhoods + the four `sig-rules` v1 signals (PR #34), WP4-UX Correlate view (this branch); standing interactive-walkthrough residual |
| WP5 | Deterministic findings | PENDING |
| WP6 | Evidence (envelope v2) / report / redaction / bundle integration | PENDING |
| WP7 | Analysis workspace UX (Patterns / Compare / Correlate / Findings) | PENDING — Patterns, Compare and Correlate views exist from their work packages; WP7 owns the shared workspace shell, Findings, and the cross-view flows |
| WP8 | Reliability, performance, invalidation, recovery, benchmarks (1M dev / 5M acceptance) | PENDING |
| WP9 | Test sweep, docs, ADRs, 0.4.0 release prep | PENDING |

Per-package rhythm (established since v0.3 W3): one PR per package,
sweep (fmt/clippy/tests/tsc+vite) before every PR, plan doc updated at
every completion, owner merges.

## Design pass (WP0 deliverable — the sections the first preflight deferred)

### Migration `0004_analysis` (workspace schema v4)

One forward-only transactional migration. STRICT tables; all timestamps
TEXT RFC 3339 UTC; all JSON columns validated at the service layer.

- `analysis_definitions` — definition_id (PK, `adef-<uuid>`),
  definition_schema_version (1), kind
  (`message_pattern|stack_fingerprint|comparison|correlation|finding_rules`),
  name, description, dataset_selection_json, query_text,
  query_language_version, query_fingerprint (nullable when all-data),
  time_strategy_json, field_selection_json (body/stack/grouping
  selectors), algorithm_id, algorithm_version, config_json
  (normalized), config_fingerprint (`acfg-<hex>`), masking_profile_json,
  thresholds_json, limits_json, created_at, updated_at, revision.
  History via the existing `record_history` discipline
  (entity_kind `analysis_definition`); editing bumps revision and never
  mutates completed runs.
- `analysis_runs` — run_id (PK, `arun-<uuid>`), definition_id + FK,
  definition_revision, semantic_fingerprint (`asem-<hex>`, deterministic
  — see identity rules), state
  (`pending|running|completed|cancelled|failed|stale`), dataset_revs_json
  (exact `[{dataset_id, dsrev}]`), query_fingerprint,
  query_language_version, bounds_json (concrete UTC-nanos intervals:
  analysis window, plus baseline/suspect for comparisons),
  algorithm_id/version, config_fingerprint, progress_stage,
  counts_json (accepted/excluded/untimestamped/invalid/truncated/
  sampled), started_at, finished_at, warnings_json, manifest_json
  (result summary + derived-data references + per-file checksums),
  error_json, invalidation_reason. Two-phase discipline: the row is
  inserted `pending` before any byte of derived data; finished exactly
  once; crash recovery (workspace open, step extending W8) finishes
  `pending|running` rows as `failed(job/interrupted)` and sweeps their
  staging directories. **Only `completed` runs are usable results;
  cancellation/failure/staleness is never an empty success.**
- `analysis_findings` — finding_id (PK, `afind-<hex>`, semantic — see
  identity), origin (`deterministic`; manual findings stay in
  `investigation_items` and are never rewritten), finding_schema_version,
  rule_id, rule_version, run_id + FK, subject_json (result identity),
  title, explanation, calculation_json (inputs+formula+thresholds),
  severity, severity_rule_json, confidence (nullable), contributing_json
  (bounded ids or reproducible scope), examples_json, state_json
  (exact/approximate/truncated + limitations), created_at (from run
  completion), revision. Re-evaluation inserts new rows keyed by a new
  run; rows referenced by evidence are retention-protected.
- `derived_artifacts` — artifact_id (PK), run_id + FK, kind
  (`pattern_membership|bucket_series|comparison_domain|correlation_edges`),
  rel_path (under `derived/analysis/<run_id>/`), row_count, byte_size,
  sha256, schema_version, created_at. Derived files are rebuildable,
  disposable, and never the only copy of anything canonical.
- Migration rollback safety: same policy as 0003 — single transaction;
  failure leaves schema v3 valid; `newer_database_is_rejected` continues
  to refuse future schemas. Existing manual items keep
  `authored_by_user=1` untouched (gate 3); no data rewrite.

### Deterministic identity and canonical serialization

- Canonical JSON serialization for identity input: object keys sorted
  lexicographically, arrays in documented order (dataset revs sorted by
  dataset_id; fields as selected order is part of config), no floats in
  identity inputs (thresholds serialized as decimal strings), UTF-8, no
  whitespace. One implementation in `logscope-case` (platform-neutral),
  property-tested.
- `semantic_fingerprint` = `asem-` + BLAKE3(canonical JSON of
  {dataset_revs, query_fingerprint, query_language_version, bounds,
  algorithm_id, algorithm_version, config_fingerprint}). Excludes run
  ids, timestamps, machine anything. Identical inputs ⇒ identical
  fingerprint ⇒ identical semantic result identities (a rerun creates a
  new `arun-` execution record with the same `asem-`).
- Pattern IDs: `pat-` + BLAKE3(canonical {algorithm_id,
  algorithm_version, masking_config_fingerprint, normalized_template}).
  Content-addressed; counts/examples/labels never affect the ID.
  Stack fingerprints: `stk-` + BLAKE3(canonical {algorithm_id/version,
  masking identity, exception_type, normalized_frames, cause_chain}).
- Comparison result IDs: `acmp-` + BLAKE3(canonical {semantic_fingerprint,
  dimension, key_identity, rule_id, rule_version}).
- Correlation edge/group IDs: `acor-` + BLAKE3(canonical
  {semantic_fingerprint, rule_id+version, normalized_key_or_temporal_rule,
  sorted participating canonical event IDs}).
- Finding IDs: `afind-` + BLAKE3(canonical {rule_id, rule_version,
  subject_semantic_id, calculation_identity}).
- Collision policy: full 32-hex digests stored; display may shorten to
  12 hex but every surface exposes copy-full-ID; the store refuses to
  merge rows whose full IDs differ (UNIQUE on full ID; a genuine BLAKE3
  collision fails loudly via constraint, never silently merges) — tested
  with crafted same-prefix IDs.

### Template extraction contract (WP2; ADR to be written with WP2)

Decision (recorded now, implemented in WP2): **pure rule-based masking
templates, no adaptive clustering.** Each record is independently
tokenized and masked by versioned deterministic rules; the template IS
the masked token sequence; the pattern is the set of records sharing it.
This is trivially order-independent (the prompt's randomized-partition /
parallelism determinism requirement), fully explainable (every mask rule
visible), and streams in one bounded pass. The known limitation —
messages differing beyond the mask rules form distinct patterns instead
of being fuzzily merged — is documented and honest; adaptive merge
(Drain-style trees) is insertion-order-dependent and is deliberately
rejected for v0.4.

- Unicode: NFC normalize; invalid UTF-8 already impossible post-ingest
  (canonical text). Case-sensitive by default (config flag to fold).
- Whitespace: runs collapse to one space for identity; line endings
  normalized to `\n`; leading/trailing trimmed. Multiline: first line is
  the message template; a stack-trace field goes to the stack
  fingerprinter, never mixed into the message template.
- Tokenization: split on whitespace; punctuation retained attached
  except bracket/quote pairs handled by the quoted-value rule.
- Built-in mask rules (each independently on/off, versioned as a set:
  `mask-set v1`): timestamp forms, UUID, trace/span hex IDs, IPv4/IPv6,
  `:port`, durations (`12ms`, `3.4s`), byte sizes, integers, decimals,
  0x-hex, file paths (slash/backslash heuristic), URLs, quoted values
  (`"…"`/`'…'` masked as one token), user-selected typed fields.
  Each masked token renders as `<num>`, `<uuid>`, `<ip>`, … in the
  template. Masking is analysis identity ONLY — the UI copy and ADR say
  explicitly it is not redaction.
- Bounds: max message length 8 KiB (longer: truncated-for-analysis flag
  on the pattern), max 512 tokens, max distinct patterns per run
  (limit, default 50k) → explicit truncated state.
- Missing/incompatible message field: excluded with counts; never an
  empty-template pattern.
- Examples: earliest, latest, peak-bucket, plus up to K distinct-resource
  examples; ties broken by canonical event ID ascending. All selection
  from data already streamed — deterministic.

### Stack fingerprint contract (WP2)

Parse per documented log-stack forms (Java, Python, Node, .NET, Go
panics) with a shared normalized frame model {module, function, location
(masked)}. Identity = exception type + ordered frames (line numbers and
addresses masked by rule) + nested-cause chain (bounded depth 8, frames
per trace 128, deeper: truncated flag participates in identity as an
explicit `+truncated` marker so a partial trace never collides with its
complete form). Malformed/partial traces fingerprint what parsed plus a
parse-quality state; distinct exception types never merge. Repeated
framework frames kept (no frame-dropping heuristics in v1; documented).

### Comparison decision table (WP3)

Classifications v1 (`cmp-rules v1`); all rates = count / duration in
seconds; different-duration windows classify on RATES only:

| Condition (after min-count guard) | Classification |
|---|---|
| baseline=0, suspect ≥ min_new_count | `New` |
| baseline ≥ min_base_count, suspect=0 | `Disappeared` |
| rate_change ≥ +threshold_rel or abs ≥ +threshold_abs | `Increased` |
| rate_change ≤ −threshold_rel or abs ≤ −threshold_abs | `Decreased` |
| within thresholds | `Unchanged` |
| below min counts both sides | `InsufficientData` |
| type conflict / cardinality guard / not in bounded union | `Excluded` (reason recorded) |

Zero-baseline relative change = `undefined` (never ∞/NaN — JSON gets the
string state, tested). Candidate domain = exact bounded union of both
windows' top-K by combined count (documented; never two independent
top-Ks), with explicit `Other` bucket and cardinality estimate surfaced
before expensive runs. Overlapping windows: refused by default; explicit
opt-in records overlap span and shared-record counts.

### Correlation model (WP4)

Confidence classes exactly as specified: `Exact` (canonical trace ID or
trace+span pair, validated at ingest; span-alone never exact),
`Correlated` (typed stable app/transport identifier, exact match on
canonical value; any normalization explicit+versioned), `Probable`
(documented resource/operation/time-proximity rule; always labeled
`Investigative lead only`). Candidate generation is key-partitioned
(never pairwise): group by validated key within time partitions, caps on
group size (default 256), groups per run (10k), edges per event (32),
total edges (250k), scanned records (limit from run config). Retry /
operational-duplicate / clock-skew / gap signals per the spec'd evidence
ladders — each a rule with ID+version whose reason lists matched and
missing fields. Ordering: event time then canonical event ID; missing
timestamps stay in an explicit undated bucket (never reordered by import
time).

### Deterministic findings (WP5)

Initial rule set (`find-rules v1`, each with thresholds in config):
new-high-severity-pattern, increased-error-pattern,
disappeared-pattern, repeated-retry-signal, operational-duplicate,
clock-skew-warning, concentrated-dimension-change. Severity = generic
mapping from canonical severity ceiling + magnitude thresholds
(documented table in the WP5 ADR); never customer/financial/security
impact claims; forbidden-wording test (`confirmed`, `root cause`,
`caused by`, `therefore`) over every generated string.

### Derived data ownership + invalidation (WP8 enforced from WP1)

Derived files live under `derived/analysis/<run_id>/` inside the
workspace, built in `staging/<job>/` and renamed at publish (same
pattern as imports). Stale when: dataset revision changed/disappeared,
query semantics or language version changed, bounds changed, algorithm/
rule version changed, masking/threshold config changed, derived file
missing or checksum-mismatched. Stale marks the RUN stale (live
references report it); history is never invalidated retroactively.
Cache reuse requires full identity verification (semantic fingerprint +
manifest checksums), never name/mtime. Retention: unreferenced runs are
deletable; runs referenced by evidence/reports/bundles are protected
(FK + check before cleanup). Recovery on open extends the W8 sweep:
interrupted runs → failed tombstones; orphan derived/staging swept;
corrupt manifests → run marked failed with reason.

### Integration design (WP6)

- Evidence: envelope **v2** adds `Pattern`, `ComparisonResult`,
  `Correlation`, `DeterministicFinding` reference+snapshot kinds; v1
  payloads decode unchanged (corpus already proves version gating).
  Resolution states extend the resolver vocabulary with
  `derived_rebuildable`, `analysis_stale`, `algorithm_unsupported`
  (registered in the error-code/vocab registries). Verification never
  reruns an analysis.
- Reports: new section kinds render analysis evidence with IDs,
  versions, formulas, thresholds, limitations; static tables only; the
  existing no-script/no-network HTML model and hostile-content proofs
  extend to templates/keys/examples.
- Redaction: the ONE projection gains awareness of analysis payloads
  (templates, examples, keys, configs are all snapshot JSON → already
  walked); preview==final stays byte-asserted.
- Bundles: analysis entries (definitions, completed-run manifests,
  bounded results) with per-analysis declared reproduction state
  (`full_within_scope|subset_only|snapshot_only`); importer preserves
  snapshot-only honestly when algorithm versions are unsupported; no
  SQLite files or raw cache directories in bundles.

### Risks

| Risk | Mitigation |
|---|---|
| Order-dependence sneaking into results via parallel merge | rule-based templates (order-free by construction); merge steps forbidden from data-dependent ordering; determinism tests run with randomized partition order + parallelism |
| Pattern cardinality explosion (mask rules too weak for a corpus) | hard distinct-pattern limit with explicit truncated state; cardinality estimate before run; mask preview shows change counts |
| Correlation edge blowup on hot keys | per-group/edge caps with truncation counts; high-cardinality guard refuses unbounded key domains |
| DuckDB float nondeterminism in aggregates | identity inputs exclude floats; rates computed in Rust from integer counts + integer nanos; documented decimal rendering |
| Derived cache divergence from SQLite truth | manifest checksums verified on use; rebuild path proven by test (delete cache → rebuild → identical result IDs) |
| Envelope v2 breaking v1 readers | version-gated decode already refuses newer versions honestly (corpus-proven); v1 kinds untouched |
| Report/bundle size growth with analysis content | bounded examples/top-K everywhere; byte limits in run config; truncation states rendered |

### Benchmarks (WP8)

Corpora: existing deterministic generator (seed 20260729) extended with
pattern-rich shapes (N template families with volatile tokens), synthetic
multiline stacks (5 language forms × volatile line numbers), retry/
duplicate/skew sequences keyed by request IDs, and a high-cardinality
attribute. 1M for development, 5M for acceptance (reusing the measured
v0.3 5M workspace where valid). Record the full environment/identity
block the prompt requires; provisional budgets set BEFORE optimization
claims; regressions tracked against them (`bench_analysis` binary
following `bench_case`'s honest-numbers pattern, cancellation latencies
included).

## Validation commands and results (this session)

```
# on main @ 47dc42b before branching (W-R final sweep, 2026-08-05):
cargo fmt --all --check                       # clean
cargo clippy --workspace --all-targets -- -D warnings   # clean
cargo test --workspace                        # 55 suites, 292 passed, 0 failed
npm run build (apps/desktop)                  # tsc && vite build, 516 ms
pwsh scripts/package-portable.ps1 -Version 0.3.0  # both artifacts, payload-equal
# P1 smoke: extracted 0.3.0 portable ZIP → logscope.exe window "LogScope", clean kill
```

## Work log — completed / pending / blocked / deferred

- Completed: preflight re-run (all 18 PASS — table above); design pass;
  43-gate matrix skeleton embedded below.
- Completed (cont.): **WP1 analysis domain** (2026-08-05). Migration
  `0004_analysis` → workspace schema v4 (additive; failed migration
  rolls back to a v3 workspace 0.3.0 opens; proven by a constructed-v3
  fixture whose manual finding stays manual). `analysis_meta.rs`:
  definitions (same-transaction `case_history` rows, optimistic
  revisions), two-phase runs (`pending → running → finished exactly
  once`; completion REQUIRES a manifest — an empty success cannot
  exist; finished runs immutable; `stale` only from `completed`, with
  the reason recorded), deterministic findings (insert-only, completed
  runs only), derived-artifact catalog (cascade), retention guard
  (finding-referenced runs refuse deletion; evidence/report/bundle
  guards extend in WP6), recovery sweep step 6 (interrupted runs →
  `failed(job/interrupted)` tombstones, reported as
  `analysis_run:<id>` in the existing recovery field — no DTO change;
  idempotent reopen proven). `logscope-case::analysis`: canonical
  identity serialization (sorted keys by construction, **floats
  refused with the offending path named**), `asem-` semantic
  fingerprints (dataset-rev order-independent), content-addressed
  `pat-`/`stk-` ids (truncation participates in stack identity),
  `acmp-`/`acor-` (participant order-independent) /`afind-` composers;
  `AnalysisKind`/`AnalysisRunState` vocab. `logscope-app::analysis`:
  definition validation through the real vocab/config/query services
  (`analysis/invalid-definition`, `query/invalid`), scope freezing
  (dataset revs + revalidated query fingerprint + concrete bounds →
  semantic fingerprint; reruns share it, executions do not),
  `begin/mark_running/complete/abort` lifecycle, staleness check
  distinguishing revised definitions, moved revisions, and
  unresolvable datasets (existence checked before fingerprinting —
  an unknown id also fingerprints an empty segment set),
  `analysis/unsupported-version` for future definition schemas. Error
  codes `analysis/*` registered; registry scan extended to the new
  namespace. Tests: +12 (identity 3 inline, domain 5, flow 4) — suite
  **304 / 0 failed**. One pre-existing test corrected under the
  established rule: `case_domain`'s v2-migration proof asserted the
  literal schema 3; the intent is "v2 data survives to the latest
  supported schema" (same fix the pre_v02 test received in v0.3 W1).
- Completed (cont.): **WP2 engine** (2026-08-05, ADR-0020).
  `logscope-case::template` — mask-set v1 (12 switchable rules, fixed
  order, decoration-aware token classification), 8 KiB/512-token bounds
  with an identity-bearing `<truncated>` token, strict profile parsing
  (unknown keys refused); whole-token quoting handled before
  decoration stripping (the corpus caught the ordering bug).
  `logscope-case::stack` — five textual forms (Java/.NET/Python/Go/
  Node) behind a fixed-order detector; frame identity drops volatile
  locations; Java frames require java-shaped `(File.java:NN)` locations
  so .NET/Node frames are never claimed (caught by test); cause chains
  bounded (8) and frames bounded (128) with identity-bearing
  truncation; malformed/partial honesty. `logscope-app::patterns` —
  one streaming pass over the FROZEN scope (run's stored bounds, never
  re-resolved), order-independent accumulator (extremes/buckets/
  resources with explicit caps + truncation flags), pre-execution
  cancellation check (the F-1 rule), summaries parquet published under
  `derived/analysis/<run>/` + cataloged (SHA-256) BEFORE the run
  completes with its manifest; paged `list_patterns` reads the parquet;
  **drill-down re-derives deterministically and refuses stale scopes**
  (`analysis/stale-run`) — the membership-parquet cache is explicitly
  WP8. Attribute text extraction goes through the model's type-tagged
  canonical JSON (plain `.as_str()` silently misses — caught by test).
  New codes `analysis/derived`, `analysis/stale-run` registered.
  Tests +17 (template 7, stack 5, pattern proofs 5): exact family
  counts incl. untimestamped, byte-identical rerun summaries, volatile
  line numbers collapsing while distinct exception types never merge,
  bounded deterministic drill-down, explicit truncation, cancellation
  never an empty success.
- Completed (cont.): **WP2-UX Pattern Explorer** (2026-08-05). Typed
  boundary: `analysis_cmds.rs` (definitions list/create with
  kind-implied algorithm identity, runs list, live staleness check,
  job-run via the established spawn/`job-event`/`analysis-finished`
  pattern with `cancel_job` reuse, paged `list_patterns`, bounded
  `pattern_records`); 6 new DTOs, bindings regenerated into BOTH
  tracked dirs. React `Patterns.tsx`: definition select/create (mask
  rules as 12 checkboxes labeled "analysis identity only — not
  redaction", stack-field input for the stack kind), run table with
  textual states + failure/cancellation/invalidation notes, stale
  banner with the live reason (drill-down disabled while stale),
  paged pattern table in the stored order (never re-sorted client
  side), detail panel with copyable full ID/template, truncation and
  parse-quality flags as text, bounded contributing-records view with
  its limit stated, empty states distinguishing no-defs / no-runs /
  zero-patterns / non-completed. Wired into App routing + home button
  (gated on a published log dataset). Explorer-jump from pattern
  examples deliberately deferred to WP6 (evidence integration owns
  restore contexts) — recorded residual. Sweep: desktop shell
  compiles, `tsc && vite build` clean (809 ms). Standing residual: no
  UI automation — human walkthrough of the Patterns screens is an
  owner action.
- Completed (cont.): **WP3 engine** (2026-08-05).
  `logscope-case::comparison` — the pure decision table (`cmp-rules`
  v1): ALL integer arithmetic (rates compared by u128
  cross-multiplication, relative change as basis-point decimal
  strings; a float can never enter a classification, identity, or
  serialized result — property-asserted); zero baseline →
  `"undefined"` + `New` behind `min_new_count`; zero suspect →
  `Disappeared` behind `min_gone_count`; both-small →
  `InsufficientData`, never `Unchanged`; different-duration windows
  classify on rates by construction; strict thresholds parsing.
  `logscope-app::comparison` — ONE compiled base query serves both
  windows (only the time predicate differs); per-side bounded
  streaming count maps (order-independent); candidate domain = exact
  bounded union of both sides (never two unrelated top-Ks);
  untimestamped records counted in an explicit third pass, never
  silently vanished; overlap refused at config parse (before any run
  record exists); strict config parse (unknown keys refused — same
  rule as mask sets); top-K remainder aggregated into the manifest
  with its counts, never dropped; results parquet (deterministic
  order: combined count desc, key asc) published + cataloged before
  the two-phase run completes; `acmp-` result ids; side-correct
  drill-down re-derives from the frozen windows and refuses stale
  runs. `analysis::begin_run_with_bounds` added so comparison runs
  freeze BOTH windows into `bounds_json` (never re-resolved against a
  moving latest-event). Dimensions v1: message_pattern,
  stack_fingerprint, severity, resource, user-selected attribute;
  **operation/outcome/event-name dimensions deferred** — they were not
  on the streaming row (`LOG_COLUMNS`) and the extension was scheduled
  with WP3-UX (deviation against gate 15; CLOSED by WP3-UX below, so
  all eight required comparison domains now exist). Tests +11
  (decision table 6, proofs 5): exact classifications from
  constructed counts, +300 %/−75 % basis points verified, identical
  rerun rows and ids, strict-config refusal, honest top-K remainder,
  side-correct drill-down + stale refusal, cancellation never an
  empty success.
- Completed (cont.): **WP3-UX** (2026-08-06). Closes the WP3
  deviation: `LOG_COLUMNS` (and the `logs.rs` page query) now carry
  `operation`, `outcome`, `event_name` — the columns have been in
  `logs_schema()` since the first storage commit, so no existing
  segment needs migrating — and the three dimensions are wired as
  `KeySource::Generic`. A record whose source never mapped the field
  is an explicit counted exclusion, NOT a shared `(none)` bucket: a
  dataset without the field would otherwise produce one enormous key
  that reads like a real distribution. `RecordDetail.event_name` now
  reads from the row instead of a duplicate SELECT column (DTO shape
  unchanged). Four commands over the typed boundary
  (`create_comparison_definition`, `start_comparison_analysis`,
  `list_comparison_results`, `comparison_records`) + 2 DTOs,
  bindings regenerated into BOTH tracked dirs; the boundary composes
  `config_json` from typed fields (the UI never hand-writes analysis
  JSON) and parses it immediately, so reversed/empty/overlapping
  windows are refused before a definition exists. React
  `Compare.tsx`: 8 dimensions, explicit UTC window inputs, a preset
  that RESOLVES to visible concrete bounds (never a stored relative
  rule), swap-windows that pre-fills a NEW definition rather than
  mutating history, thresholds as integers (basis points labeled),
  run table with per-side accepted counts, exclusion line
  (missing-field / malformed stacks / untimestamped / key-limit /
  beyond-top-K remainder), stale banner disabling drill-down, paged
  results in the stored order with a page-scoped view filter labeled
  as a display filter, per-row bounded count bars whose exact
  integers are in adjacent columns, and a detail panel printing the
  cross-multiplication formula, both durations, and the basis-point
  value. Tests +1 proof: operation/outcome classified from exact
  counts (30→12 = `"-6000"`, 4→20 = `"40000"`), 12 unmapped records
  counted as exclusions, side-correct drill-down, and an unmapped
  `event_name` completing with zero keys and all 78 records counted
  as excluded — an empty result that says why.
- Completed (cont.): **WP4a** (2026-08-06), the exact/correlated half
  of correlation. `logscope-case::correlation` (`corr-rules` v1):
  three confidence classes and no others; **there is deliberately no
  span-alone selector**, so the type system — not a runtime check
  someone can forget — makes the mistake unrepresentable, and the
  `span_id` config name is refused BY NAME with the reason and the
  fix. Exact requires canonical telemetry identity (32/16 lowercase
  hex, never all-zero), re-checked in the rule layer so the guarantee
  lives next to the rule that depends on it; invalid originals stay
  visible as `trace_id.raw` attributes but never reach the canonical
  column, hence never a group (segment-level assertion, both halves).
  Normalization is explicit, versioned, off by default, and REFUSED
  outright on canonical identifiers; on correlated keys the steps that
  actually changed a value travel into the reason. Every explanation
  carries "does not establish ordering, parent/child structure,
  completeness, or causation", trace groups add "not a reconstructed
  trace", and a forbidden-wording assertion covers every generated
  string. `logscope-app::correlation`: candidate generation is
  **key-partitioned, never pairwise** (one bounded streaming pass into
  per-key buckets); edges connect consecutive records only; caps on
  events/group (256), groups/run (10k), edges/event (32), total edges
  (250k) each report what they dropped; `min_group_size` ≥ 2 refuses
  the "group of one" that is not a relationship; ordering is event
  time then record ID (a deterministic content address, so the
  tie-break is stable across machines) and **undated records are
  counted but never sequenced** — ordering them by import time would
  manufacture a story. Groups + edges parquet cataloged with SHA-256
  before completion; paged listings; drill-down re-derives from the
  frozen scope and refuses stale runs. Tests +15 (rule model 7, proofs
  8). One product bug found by its own test: the group reason
  recomputed normalization from the STORED key, which is already
  normalized, so it reported "changed nothing" exactly when
  normalization had done the most work — the applied steps are now
  captured during the pass and reported in rule order.
- Completed (cont.): **WP4b** (2026-08-08, ADR-0021), the probable half
  of correlation plus the four behavioural signals.
  `logscope-case::signals` (`sig-rules` v1) gives each signal its own
  rule ID and an evidence ladder — `documented` when the source stated
  the fact in a typed field, `corroborated` when several typed fields
  agree, `indicative` for a single weak indicator — and **only
  `documented` is reported as an observation**; everything else carries
  `Investigative lead only.` Each result names the fields that matched
  *and the fields the rule wanted but did not find*, so a reader sees
  how much of the evidence was actually present. **Retry has nowhere to
  put a message**: `RetryFacts` carries attempt/operation/outcome and
  no text at all, so "these two records say the same thing" cannot
  become a retry however alike they read (gate 24 by construction
  rather than by check). **Ingestion duplicates are classified, not
  filtered**: `classify_duplicate` returns a distinct `Ingestion`
  variant for the same source position — and for a same-source pair
  with no record numbers, where distinctness cannot be shown — so the
  import-side fact stays visible in its own run counter instead of
  vanishing or masquerading as the system repeating itself (gate 25).
  Skew and gap carry both original timestamps, the measured delta and
  the tolerance, and **there is no field in `TimeObservation` to return
  an adjusted time in** (gate 26); a gap can never reach `documented`,
  because absence of records is not a record of absence, and says so
  ("collection failures, filtering, sampling, retention"). Ingest
  already decides these things once, so analysis reads its verdict
  rather than re-deriving it: `QualityFlag::DuplicateRecord` excludes a
  record from the operational-duplicate rule, `TimezoneAssumed` /
  `TimestampUnparsed` drop a time claim from `corroborated` to
  `indicative`, and `OutOfOrderTimestamp` corroborates a skew finding.
  Probable neighborhoods live in `logscope-case::correlation`:
  `ProbableNeighbor` has **no confidence field and no constructor that
  sets one** — `confidence()` returns `Probable` and there is no other
  value it could return — so gate 22 is a property of the type; a
  required field the candidate lacks is a rejection, never a wildcard;
  ordering is absolute distance from the anchor first, so a truncated
  neighborhood drops its least relevant members rather than an
  arbitrary tail (gate 23). `logscope-app::correlation`: signals are
  computed in the same bounded pass, with duplicates found by
  **bucketing members on a message digest** rather than comparing every
  pair — inside a 256-member group the pairwise form is 32,640
  comparisons for the same answer, and pairwise growth is what this
  module refuses everywhere else (gate 27). Neighborhoods are
  deliberately **not** materialised: a neighborhood is anchored to one
  record a person selected, so it is a two-pass drill-down that narrows
  the run's frozen window to the anchor's tolerance interval and lets
  the engine's own time filter do the work. Third artifact
  (`correlation_signals.parquet`) cataloged with SHA-256 before
  completion; `RESULTS_SCHEMA_VERSION` → 2, so a WP4a-era run is
  refused with the re-run instruction rather than returning an empty
  page — "none were computed" and "none were found" must not look
  alike. Tests +21 (signal rules 9, neighborhood rules 5, proofs 7).
  One test expectation corrected against the code rather than the
  reverse: a second gap was real (the hour between the neighborhood
  corpus's last two records), not a bug.
- Completed (cont.): **WP4-UX** (2026-08-08), the Correlate view —
  seven commands, six DTOs, and `Correlate.tsx`. The view never decides
  that something is a relationship: confidence classes, evidence
  strengths and limitation strings are displayed as the run recorded
  them, and where the engine called something an investigative lead the
  UI labels it one. `create_correlation_definition` composes
  `config_json` from typed fields and calls `CorrelationConfig::parse`
  immediately, so `span_id` as a key, normalization on a canonical
  identifier, a zero gap threshold, or an unknown signal name are all
  refused **before a definition row exists**. The form disables the
  normalization controls for exact keys, and a proof pins that the
  engine refuses them regardless — the disabled control displays the
  rule, it is not the rule. Because that composition lives in the
  desktop crate (outside the workspace test run), the exact JSON shapes
  it emits are pinned by a test in `logscope-app` rather than left to
  break at runtime. Durations (tolerances, thresholds) cross the
  boundary as `number` and absolute event times as `bigint`: 2^53 ns is
  ~104 days, so a duration is safely exact while an epoch-nanosecond
  value is not. New chips reuse the shared theme tokens from #33, so
  `npm run check:contrast` covers them with no new pairs, and severity
  of evidence is never carried by colour alone — every chip prints its
  own name. Tests +7: one hand-written (the composed-config shapes) and
  six ts-rs binding exports generated by the new DTOs. Workspace total
  377 → 384.
- Pending: WP5–WP9.
- Known cost, deferred to WP8 by design: per-member signal detail is
  bounded by `max_events_per_group × max_groups` (256 × 10,000) and is
  held in memory for the duration of a run. That is the same bound
  WP4a's sequence already carried, roughly tripled; interning the
  repeated dataset/source strings is the obvious fix and belongs with
  the rest of the reliability work.
- Blocked: none.
- Deferred (unchanged): AI/LLM anything, CLI/Agent API/MCP, metrics,
  traces (IDs on logs are correlation keys only), live tailing/OTLP
  productization, alerts, dashboards, ML/fuzzy anything, arbitrary SQL,
  executable user rules, bundle encryption/signing.

## v0.4 acceptance matrix (final — to be completed at milestone end)

The 43 gates, verbatim from the milestone contract; populated as work
packages land. `BLOCKED` ≠ `PASS`.

| # | Gate (abbreviated where long; contract wording governs) | Verdict |
|---|---|---|
| 1 | Actual v0.3 baseline + acceptance report inspected; prerequisites recorded | PASS (this document) |
| 2 | v0.3 workspace migrates transactionally to v0.4, reopens, searchable, preserves investigations/evidence/reports/bundles, no re-import | PENDING (WP1) |
| 3 | Existing user-authored findings remain explicitly manual with history | PENDING (WP1) |
| 4 | Definitions/runs persist stable IDs + exact identities across close/reopen | PENDING (WP1) |
| 5 | Cancelled/failed/partial/stale runs never appear as complete success | PENDING (WP1) |
| 6 | Derived data rebuildable/versioned/invalidated; never mutates canonical; delete+rebuild safe | PENDING (WP1/WP8) |
| 7 | Identical inputs ⇒ identical IDs/memberships/calculations/orderings across reruns, parallelism, randomized partitions, reopen, cold/warm | PENDING (WP2+) |
| 8 | Config/algorithm/rule/query/revision changes ⇒ explicit new identity or stale, never silent change | PENDING |
| 9 | Template extraction has documented tokenization/normalization/masking/size/Unicode/locale/merge semantics | PENDING (WP2 + ADR) |
| 10 | Masking previewable, versioned, bounded, analysis-only (not export redaction) | PENDING (WP2) |
| 11 | Pattern results expose IDs/templates/stats/examples/dimensions/exclusions/limitations/drill-down | PENDING (WP2) |
| 12 | Stack fingerprints distinguish materially different traces, normalize volatile fields, preserve malformed states, deterministic | PENDING (WP2) |
| 13 | Pattern lists/examples/buckets/drill-down paged/bounded; no full memberships in React | PENDING (WP2/WP7) |
| 14 | Baseline/suspect windows preserve exact bounds/semantics/duration/timezone/overlap/missing-ts counts, one base query | PENDING (WP3) |
| 15 | All required comparison domains expose counts/rates/formulas/thresholds/classifications/limitations | PENDING (WP3) |
| 16 | Zero baseline/suspect, different durations, insufficient data, high cardinality, truncation, type conflicts — no div-by-zero/hidden coercion/misleading classes | PENDING (WP3) |
| 17 | Classifications pass deterministic regression fixtures incl. predefined false positives | PENDING (WP3) |
| 18 | Every comparison result drills to correct baseline/suspect records via authoritative semantics | PENDING (WP3) |
| 19 | Valid trace IDs / trace+span pairs ⇒ exact relationships without trace reconstruction; span-alone ⇒ never | PASS (WP4a) — no `SpanId` variant exists, `span_id` refused by name with the fix; `exact_groups_come_only_from_canonical_telemetry_identity`, malformed IDs stay visible as `trace_id.raw` but never group; every exact reason carries the "not a reconstructed trace" limitation |
| 20 | Correlated relationships only under visible typed matching rules | PASS (WP4a) — typed selectors only; normalization off by default, refused on canonical identity, and the steps that actually changed a value travel into the reason |
| 21 | Probable relationships expose fields/constraints/tolerance/quality/rule/limitation | PASS (WP4b) — all six are fields of `ProbableNeighborhood`, asserted individually in `a_neighborhood_is_probable_bounded_and_states_all_of_its_own_terms` |
| 22 | Time proximity alone never Exact/Correlated; nothing presented as causation | PASS (WP4b) — `ProbableNeighbor` has no confidence field and `confidence()` can return only `Probable`; proximity-only is allowed but says so; forbidden-wording assertion over every group, signal and neighborhood string |
| 23 | Groups/neighborhoods/sequences: stable ordering, preserved timestamps, truncation, limits | PASS (WP4a+WP4b) — event time then record ID within groups, absolute distance first within neighborhoods so truncation drops the least relevant; undated records counted, never sequenced; `admitted`/`truncated` reported |
| 24 | Retry signals require documented evidence; similar text alone never a confirmed retry | PASS (WP4b) — `RetryFacts` has no message field at all, so text cannot be retry evidence by construction; only an advancing typed attempt counter reaches `documented`, everything weaker is labelled an investigative lead |
| 25 | Ingestion duplicates ≠ operational duplicate-message signals | PASS (WP4b) — `DuplicateClass::Ingestion` is a distinct outcome (same source position, or a same-source pair whose distinctness cannot be shown), `QualityFlag::DuplicateRecord` excludes a record from the rule, and both are reported in their own `ingest_duplicates_excluded` counter. Residual: the ingest-flag path is unit-proven; a single import cannot produce the flag, so it has no integration case |
| 26 | Skew/gap indicators expose originals/deltas/tolerances/quality; never rewrite time | PASS (WP4b) — `TimeObservation` carries both originals, the delta, the tolerance and both quality values, and **has no field for an adjusted time**; inferred timestamps drop the claim to `indicative`; a gap can never reach `documented` |
| 27 | No unbounded pairwise joins; high-cardinality + cancellation tests pass | PARTIAL — WP4 half PASS: candidate generation is key-partitioned, duplicates are found by digest bucketing (never pairwise), every cap reports what it dropped, and cancellation is proven never to be an empty success. WP8 still owns the 1M/5M benchmark and the per-member memory bound recorded above |
| 28 | Findings preserve rule/inputs/calculation/thresholds/severity/confidence/records/limitations/origin | PENDING (WP5) |
| 29 | No finding implies unverified root cause/impact/mitigation/resolution/validation | PENDING (WP5) |
| 30 | Patterns/comparisons/correlations/findings pinnable through real UI flows as versioned evidence | PENDING (WP6/WP7; carries the v0.3 interactive-walkthrough residual) |
| 31 | Analysis evidence verification distinguishes all resolution states without rerunning/rewriting | PENDING (WP6) |
| 32 | Reports capture exact revisions, render formulas safely, execute nothing, zero network | PENDING (WP6) |
| 33 | Preview/final projection equivalence extends to every analysis value; no hidden leaks | PENDING (WP6) |
| 34 | Bundle round trip: reproduce one analysis at declared scope, preserve another snapshot-only, resolve evidence, regenerate report | PENDING (WP6) |
| 35 | Malicious/excessive definitions/masks/regexes/manifests/entries/cardinalities fail safely | PENDING (WP6/WP9) |
| 36 | 1M dev + 5M acceptance benchmarks complete with the full measured set | PENDING (WP8) |
| 37 | Result pages/drill-down meet provisional budgets, or gate marked failed without post-hoc budget edits | PENDING (WP8) |
| 38 | Log-only workspace completes Explorer → analysis → evidence → report/bundle offline | PENDING (WP9 e2e) |
| 39 | Runtime no-outbound-network tests pass | PENDING (re-run at WP9) |
| 40 | fmt/lint/unit/property/integration/migration/UI/security/e2e/benchmark/packaging tests pass | PENDING (WP9) |
| 41 | Setup + portable from one payload, byte-equivalent, offline, no admin/signing | PENDING (WP9; machinery proven at 0.3.0) |
| 42 | macOS build boundary preserved | PENDING (continuous via CI) |
| 43 | No organization-specific concepts or real operational data | PENDING (re-scan at WP9) |

Continuation (fresh session): read this file first. Next task — **WP1**
per the "Pending" entry in the work log above; the design-pass sections
are the binding contracts (identity rules, schema shapes, run lifecycle,
error-code namespace `analysis/*`). The original milestone contract's
full wording is authoritative for gate language; this document embeds
everything needed to proceed without transcript archaeology.
