# LogScope v0.0 / v0.1 Implementation Plan — Living Status Document

Updated: 2026-07-29 (session 1, continued)
Branch: `feature/logscope-v0.0-v0.1`

This document tracks the implementation of Milestone 1 (v0.0 Foundation and
compatibility spike) and Milestone 2 (v0.1 Universal Offline Log Case
ingestion). It is updated at every checkpoint and is the handoff point for
continuing work.

## Inspected baseline

- Repository at start: `README.md` only (commit `fdb375c`), branch
  `master`, clean. No `CLAUDE.md`, no `AGENTS.md`, no
  `LogScope-Development-Roadmap-v1.0.md` — the roadmap requirements were
  taken from the driving prompt (portable-first revision).
- Machine: i9-12900KF (24 threads), 64 GB RAM, NVMe, Windows 11 Pro
  10.0.26100. Toolchain: rustc/cargo 1.97.1 stable (rustup repaired via
  winget — the machine had a toolchain dir but no proxies/rustfmt/clippy),
  Node 22.6.0 / npm 10.8.2, MSVC Build Tools, WebView2 Runtime
  150.0.4078.99 present (evergreen).
- Key locked dependency versions: duckdb 1.10505.0 (bundled; static
  json+parquet; no fts/httpfs), rusqlite 0.40.1 (bundled, FTS5),
  arrow/parquet 59.1, tonic 0.14.6 / prost 0.14.4 /
  opentelemetry-proto 0.32.0, tauri 2, ts-rs 12, zip 8.6, rand 0.9.

## Repository / module layout (as built)

```
crates/logscope-model         canonical model, deterministic IDs/hashes (ADR-0004)
crates/logscope-normalize     timestamps (+DST), severity map, attr helpers
crates/logscope-workspace     layout, manifest, SQLite metadata, migrations,
                              staging + atomic publish + crash recovery (ADR-0003/0005)
crates/logscope-jobs          background jobs: progress/pause/cancel/panic isolation
crates/logscope-ingest        Import Profile contract draft, source contracts,
                              streaming JSONL/CSV readers, log normalizer (ADR-0008)
crates/logscope-store         Parquet segment writers (logs/metrics/spans), FTS5 index
crates/logscope-query         hardened DuckDB engine, bounded log pages, budgets/
                              cancellation, span graph, metric rollup (ADR-0006)
crates/logscope-otlp          EXPERIMENTAL OTLP spike: HTTP pb/JSON + gRPC receivers
                              (loopback, off by default, bounded), JSONL import,
                              lossless conversion, equivalence goldens (ADR-0009)
crates/logscope-app           import service (staged/atomic), typed command DTOs
crates/logscope-testsupport   deterministic corpus generators + bench harness (ADR-0010)
apps/desktop                  React/TS UI (Vite) + src-tauri shell (typed commands)
fixtures/                     synthetic golden fixtures (see fixtures/README.md)
docs/adr/0001..0010           accepted ADRs
docs/benchmarks/              measured baseline report
scripts/package-portable.ps1  portable ZIP assembly (ADR-0002)
.github/workflows/ci.yml      fmt/clippy/tests Windows+Linux+macOS, macOS shell
                              build smoke, portable packaging job
```

## Work-package status (v0.0)

| # | Work package | Status |
|---|--------------|--------|
| 1 | Workspace scaffolding, module boundaries | DONE (commit e80f73f) |
| 2 | Canonical model + determinism goldens | DONE (18 tests) |
| 3 | Normalizer (timestamps/DST, severity, attrs) | DONE (14 tests) |
| 4 | Jobs (progress/pause/cancel/panic) | DONE (5 tests) |
| 5 | Workspace layer (manifest, migrations, staging, recovery) | DONE (7 tests) |
| 6 | Offline query probes (static parquet+json, no fts/httpfs, interrupt) | DONE (6 tests) |
| 7 | Store: schemas + segment writers + FTS5 | DONE (2 tests) |
| 8 | Ingest: profiles, contracts, streaming readers, normalizer | DONE (7 tests + 7 fixture goldens) |
| 9 | Query services: log pages, budgets, cancellation, span graph, rollup | DONE (5 analysis proofs) |
| 10 | App import service + vertical proof (import→query→FTS→reopen→query, cancel, duplicates, determinism) | DONE (3 integration tests, commit 4f8cbc1) |
| 11 | OTLP spike + 4-transport equivalence + hardening + trace fixtures | DONE (8 tests) |
| 12 | Fixtures set + inventory | DONE (20 files) |
| 13 | Deterministic generators + bench harness + measured baseline | DONE (3 generator tests; runs recorded in docs/benchmarks) |
| 14 | Tauri shell + typed DTO boundary + minimal React UI | CODE COMPLETE — desktop build in progress |
| 15 | Portable ZIP packaging script + portable README + notices | DONE (script; execution pending desktop release build) |
| 16 | CI workflows incl. macOS core+shell smoke | DONE (definitions; not executed here — no runners) |
| 17 | ADRs 0001–0010 | DONE |
| 18 | v0.0 acceptance matrix with evidence | IN PROGRESS (below) |

v0.1 has not started (first v0.1 task recommendation at the end).

## v0.0 acceptance matrix (current evidence state)

| Gate | Status | Evidence |
|------|--------|----------|
| Clean Windows env extracts + launches portable ZIP without install/admin/download | BLOCKED (needs a separate clean machine) | script + manifest ready; local smoke pending |
| Artifact is an ordinary ZIP (no MSI/NSIS/SFX/bootstrapper) | PENDING RUN | `scripts/package-portable.ps1` produces plain ZIP + sha256 + manifest |
| Launch from paths with spaces/non-ASCII | PENDING RUN | planned local smoke |
| App dir read-only; mutable state in app-config dir | PASS (by construction) | recent list under `%APPDATA%\org.logscope.desktop`; no writes next to exe; workspaces user-chosen |
| First launch performs no WebView2 install/download | PASS on this machine (evergreen present; no installer invoked) / BLOCKED for clean machine without fixed runtime bundled | ADR-0002 mechanism implemented |
| Launch/create/import/query/close/reopen with networking disabled | PENDING RUN (manual) | vertical proof passes programmatically |
| No runtime outbound access | PASS (design + probes) | no network deps in runtime path; DuckDB hardened; CSP 'self'; probes green |
| Native libs + DuckDB extensions load from app bundle | PASS | static bundled build; `offline_probe.rs` |
| Packaging without signing credentials | PASS (by design) | script has no signing step |
| macOS CI job defined | PASS (definition) / BLOCKED (execution: no macOS runner here) | `.github/workflows/ci.yml` `core-macos` |
| OTLP receiver ≡ OTLP JSONL canonical values | PASS | `equivalence.rs` (4 transports) |
| Unknown attributes + schema URLs survive | PASS | `unknown_attributes_and_schema_urls_survive` |
| 1M logs + 1M metric points without whole-input memory | PASS | bench: 175 MiB peak vs 240 MiB input; metrics peak scale-invariant (513 vs 489 MiB at 4× input) |
| Orphan/linked/incomplete/sampled/duplicate/out-of-order spans survive reconstruction, no fabrication | PASS | `span_graph_preserves_problems_without_fabrication`, trace fixtures |
| Cancelling import/query leaves workspace valid | PASS | vertical proof + analysis proofs + bench cancel (4.7 ms, clean) |
| Interrupted staged import recovered/discarded on restart | PASS | `interrupted_import_is_discarded_on_reopen` |
| fmt/clippy/unit/integration/packaging checks | IN PROGRESS | fmt+clippy clean at last full run; full re-run pending after desktop build |

## Validation commands (all executed this session)

```
cargo test --workspace                          # 60+ tests green at 4f8cbc1
cargo test -p logscope-otlp                     # equivalence + hardening + fixtures
cargo test -p logscope-ingest --test fixture_goldens
cargo fmt --check && cargo clippy --workspace --tests
cargo run --release -p logscope-testsupport --bin bench_import -- logs 1000000
cargo run --release -p logscope-testsupport --bin bench_import -- metrics 1000000
cargo run --release -p logscope-testsupport --bin bench_import -- spans 250000
cargo run --release -p logscope-testsupport --bin bench_import -- cancel-logs 1000000
npm run build                                   # apps/desktop, 152 KB local JS
cargo test -p logscope-app export_bindings      # ts-rs bindings
```

Headline measurements: see `docs/benchmarks/2026-07-29-v0.0-baseline.md`.

## Known limitations / open items

- Desktop UI blocks overview/query commands while an import runs (the
  workspace moves into the job); v0.1 lifts this with a shared handle.
- OTLP network receivers retain envelope hashes, not raw bytes (spike
  limitation; ADR-0009).
- Fixed WebView2 runtime is not downloaded/bundled in this environment; the
  packaging script supports `-WebView2FixedDir` and the manifest records
  which flavor was built.
- v0.1 scope (workspace lifecycle UI, text/multiline/UTF-16 parsers,
  archives, rotation families, full profile set, import wizard, 5M
  benchmark) not started.

## Next recommended tasks

1. Finish desktop build → release build → run `package-portable.ps1` →
   local smoke launch (spaces/non-ASCII path) → complete the acceptance
   matrix.
2. v0.1 WP1: workspace lifecycle (rename/archive/managed-copy) on the
   existing schema.
3. v0.1 WP2: text/multiline framing reader + UTF-16 decode via
   `encoding_rs` + gzip/zip safe extraction with limits.
