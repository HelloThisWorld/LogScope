# LogScope v0.3 Implementation Plan — Living Status Document

Updated: 2026-07-30 (session start)
Branch: `feature/logscope-v0.3` (from `main` @ `b9d655f`; local only, no push)

v0.3 goal: a coherent local investigation workbench — durable investigations
with hypotheses and typed items, versioned evidence pinned from the Explorer,
an investigation timeline, safe offline Markdown/HTML reports behind a
redaction preview, and a portable case bundle that round-trips offline.

## Inspected baseline

- `main` @ `b9d655f` = v0.2 complete at version 0.2.0 (PR #1 merged
  2026-07-29). Authoritative v0.2 record:
  `docs/development/v0.2-implementation-plan.md` (final acceptance matrix).
- v0.2 verdict: 20 of 21 gates PASS; **gate 19 setup.exe half BLOCKED** —
  the graphical extractor was never built (v0.1 was skipped entirely; the
  portable ZIP half passed: 17.2 MiB, SHA-256 recorded, smoke-launched).
- v0.1 never existed; schema lineage is v1 (`0001_init`) → v2
  (`0002_explorer`). "Migrate from the latest valid v0.2 schema" therefore
  means **schema v2**.
- CI (PR #3, orthogonal to this branch) moved to hosted runners with
  branch-independent warm caches; packaging job is dispatch-only.

## Prerequisite acceptance matrix (mandatory preflight)

Commands run on this machine (i9-12900KF, Windows 11 Pro 10.0.26100),
2026-07-30, branch `feature/logscope-v0.3` @ `b9d655f` tree:

Sweep result (2026-07-30): `cargo fmt --check` clean; `cargo test
--workspace` exit 0 — **169 tests across 38 suites, 0 failures** (incl. 5
property suites). Individual evidence below.

| # | Prerequisite | Status | Evidence |
|---|---|---|---|
| P1 | Desktop app launches in dev environment | PASS | packaged 0.2.0 `logscope.exe` smoke-launched from `dist-portable/`: process alive after 5 s, window title `LogScope`, terminated cleanly (2026-07-30) |
| P2 | Real v0.2 workspace create/open/close/reopen/migrate | PASS | `lifecycle` suite (13), `explorer_flow_import_query_save_reopen`, `pre_v02_workspace_migrates_and_stays_searchable` — all green in sweep |
| P3 | Built-in Import Profile completes real log-only import | PASS | `explorer_flow` (ES export profile end-to-end), `fixture_goldens` green |
| P4 | Explorer free-text + structured queries via authoritative service | PASS | `explorer_service` 14 proofs, `language` 24 conformance + property suites green |
| P5 | Results/histogram/facets/summaries/context/saved/export share compiled semantics | PASS | `shared_filter_page_counts_histogram_facets_agree`, `export_is_bounded_streamed_atomic_and_cancellable` green |
| P6 | Canonical events: stable IDs survive close/reopen | PASS | deterministic `log-<32hex>` content IDs (`determinism` suite green); reopen covered by P2 tests |
| P7 | Canonical events retain typed fields/raw/Resource/Scope/ts-quality/provenance | PASS | `RecordDetail` completeness (v0.2 gate 7 test) green |
| P8 | Dataset & source fingerprints durable; verified/changed/missing/unavailable distinguishable | PASS | `source_files.content_hash` (full-file BLAKE3) + size checks; `source_context_uses_source_order` covers changed/missing reporting |
| P9 | Source context honestly separates canonical vs original bytes | PASS | v0.2 gate 8: raw excerpts labeled vs canonical copies; test green |
| P10 | Long query/export ops: progress, cancel, safe cleanup | PASS | `cancellation_and_timeout_leave_engine_usable`, export-cancel cleanup green; v0.2 measured ack 16–21 ms |
| P11 | v0.2 workspace migrations + recovery tests pass | PASS | `migrations_apply_and_are_idempotent`, `newer_database_is_rejected`, lifecycle recovery tests green |
| P12 | Runtime no-outbound-network tests pass | PASS | `offline_probe` + `receiver_hardening` suites green |
| P13 | Setup package + portable ZIP from one byte-equivalent payload | **FAIL (inherited)** | `tools/logscope-setup` does not exist (v0.2 gate 19 BLOCKED; v0.1 packaging debt). Portable half PASS (0.2.0 ZIP built/verified/launched). Not a foundation prerequisite — no evidence-layer guarantee depends on it; fixed in W-R since v0.3 gate 30 requires it anyway. |
| P14 | Platform-neutral core viable for macOS | PASS | CI `core-macos` (hosted macos-latest: shared-core tests + `npm ci`/build + desktop shell build) SUCCESS on PR #3, 2026-07-30 |

Preflight decision per milestone rules: P13 is the only gap and it is
packaging debt, not a false guarantee under the evidence layer —
implementation proceeds with P13 scheduled inside this milestone (W-R).

## Existing modules and contracts to reuse

| Module | Reused for v0.3 |
|---|---|
| `logscope-model` | `LogRecord` (`log-<32hex>` deterministic IDs), `IngestProvenance` (raw_hash, locator, parser/profile versions, QualityFlags), `stable_id`/`Digest` for fingerprints |
| `logscope-workspace` | `MetaDb` (WAL SQLite, forward-only migrations — add `0003_investigations`), repo-per-domain pattern (`explorer_meta.rs` → new `case_meta.rs`), staging/publish/recovery, `workspace_info` KV |
| `logscope-query-lang` | language v1 validation + canonical AST JSON + `qry-` fingerprints for evidence query references |
| `logscope-query` | authoritative service: `query_page/counts/histogram/facets/field_summary/source_context`, `fetch_record(_detail)`, `stream_query`, `TimeStrategy`/`resolve_window`, cancel registry — evidence resolution calls these; add batched record fetch (no N+1) |
| `logscope-jobs` | cancellable jobs w/ progress → batch verification, report generation, bundle export/import |
| `logscope-app` | service glue + ts-rs DTO boundary (`dto.rs`), stable error codes (`code()`) — add case/evidence/report/bundle services + DTOs |
| `apps/desktop` | Explorer UI gains pin actions; new Investigation view; typed `api.ts` |
| `logscope-store` / Parquet | untouched: bulk data stays immutable; bundles carry a *subset copy* written through a documented trusted path |
| `logscope-testsupport` | corpus generators + mem tracking → 1,000-evidence and 5M-workspace benchmarks |

New code:
- `crates/logscope-case` (new, platform-neutral): typed investigation /
  evidence-envelope / timeline / report / redaction / bundle domain,
  version-gated (de)serialization, deterministic Markdown + self-contained
  HTML rendering, redaction projection, bundle manifest + planner +
  container I/O. No DuckDB/Tauri dependency.
- `crates/logscope-workspace/src/case_meta.rs`: transactional repositories +
  revision history for all v0.3 control-plane tables.
- `crates/logscope-app/src/case.rs` (+`report.rs`, `bundle.rs`): orchestration
  (pin flows, batch resolver, report/bundle jobs) over query service + repos.
- `apps/desktop/src-tauri/src/case_cmds.rs` + React Investigation UI.
- `tools/logscope-setup` (release prep): graphical extraction package
  clearing inherited gate-19 debt.

## Ordered work packages and live status

| # | Package | Status |
|---|---|---|
| W0 | Preflight + this plan | DONE (83c84c8) |
| W1 | Migration 0003 + investigation domain (investigations, hypotheses, items, scope refs, history, conflicts) + `logscope-case` skeleton | DONE |
| W2 | Evidence contract: envelope v1 (6 kinds), pin services, batched cancellable resolver + integrity states, ordering/grouping/supersession | DONE (storage 7c73a3d; pin services + resolver + ADR-0017 this commit) |
| W3 | Investigation + evidence UX (workspace view, pin dialogs, evidence panel, jump-back) | DONE (this branch) — one honest residual: no UI automation exists, so behaviour is proven at the service/DTO layer and by `tsc`; the interactive flows have not been driven by a human yet |
| W4 | Investigation timeline (merge + markers + undated section) | DONE (this branch) — same UI-automation residual as W3 |
| W5 | Reports: definitions, consistent snapshot, deterministic Markdown, self-contained HTML, staging/atomic/checksum | DONE (this branch) — same UI-automation residual as W3/W4; redaction integration lands with W6 |
| W6 | Redaction/disclosure projection + preview contract (shared with bundles) | DONE (this branch) — heuristic secret detection deliberately not built (contract lists it as optional best-effort only); same UI-automation residual |
| W7 | Portable case bundle: planner, writer, hostile-import validation, isolated-workspace import | DONE (this branch) — residuals recorded in the work log: imported parquet is an inert file (not a registered dataset); under a disclosure profile no raw data is exported at all |
| W8 | Reliability: recovery paths, 5M/1,000-evidence benchmarks, cancellation latencies | DONE (this branch) — measured numbers in `docs/benchmarks/2026-08-05-v0.3-case-baseline.md`; verify-cancel latency (~0.7 s, phase-bounded) recorded as a known property, not hidden |
| W9 | Test completion sweep: property/fuzz corpora, e2e workflows, regression | DONE (this branch) — 292 tests; the sweep caught and fixed two real defects (posture-array bypass in the disclosure compiler; hypothesis links/state lost by bundle round trip) |
| W-R | Release prep: `tools/logscope-setup` + byte-equivalence (gate-19 debt), docs, ADRs, 0.3.0, both artifacts, final matrix | PENDING |

## Schema design (migration `0003_investigations`, workspace schema v3)

One forward-only migration, applied in one transaction by the existing
runner. **Pre-release iteration policy:** schema v3 has shipped in no
released build; until 0.3.0 is released, 0003 may be amended in place on
this branch. After release it is frozen like 0001/0002.

Conventions carried over: STRICT tables, TEXT ULID-free opaque IDs with
type prefixes, RFC3339 TEXT wall-clock stamps, unix-nanos INTEGER event
instants, `*_json` typed payload columns.

ID scheme (UUID v4, never content hashes — identity is independent of
title/path/row/order): `inv-` investigation, `hyp-` hypothesis, `item-`
note/task/finding/question, `iscope-` scope ref, `ev-` evidence, `evg-`
evidence group, `mark-` timeline marker, `rep-` report definition, `art-`
generated artifact, `red-` redaction profile, `bnd-` bundle export,
`bimp-` bundle import record.

Tables (full DDL in the migration file):

- `investigations` — schema_version, title, description, status
  (`open|investigating|mitigated|resolved|archived`), severity (generic
  documented scale `sev1..sev4|none`), owner_text (exactly as typed; never
  inferred from OS/git), tags_json, created/updated/status_changed stamps,
  optional incident/mitigation/resolution instants + relevant window
  (half-open nanos), `revision` (optimistic concurrency token).
- `investigation_scope_refs` — typed rows: `dataset` (dataset_id +
  captured `dataset_revision` fingerprint), `resource_selector`
  (selector_json), `saved_query` (saved_search_id), `embedded_query`
  (query_json w/ language_version), `time_window`, `label`. Fail honestly:
  unresolvable targets are reported, never name-matched.
- `hypotheses` — statement, rationale, state
  (`unverified|supported|rejected|confirmed` — all transitions manual),
  position, revision. `hypothesis_evidence` join table for linked evidence.
- `investigation_items` — kind `note|task|finding|question`; content;
  task_status `todo|doing|done|dropped`; question_status
  `open|answered|deferred`; `authored_by_user` = 1 for every v0.3 row;
  reserved `finding_provenance_json` (NULL in v0.3) lets deterministic
  v0.4 findings carry rule/calculation provenance without changing manual
  findings' meaning; position; archived flag; revision.
- `evidence` — envelope: envelope_version (=1), kind
  (`event|selection|query|explorer_group|histogram_interval|item_ref`),
  signal (`log|manual`), title, annotation, relevance,
  captured_investigation_revision, group_id, position,
  supersedes_evidence_id, archived (normal removal = archive + history
  tombstone; hard delete is not a UI operation), resolver_state +
  resolver_detail_json + last_verified_at, `reference_json` (typed live
  canonical reference), `snapshot_json` (bounded captured snapshot),
  revision. Live reference and captured snapshot are separate columns and
  never rewritten by verification.
- `evidence_groups` — named local ordering groups.
- `timeline_markers` — kind
  (`deployment|config_change|operator_action|custom`), label, description,
  at_nanos (UTC; NULL = undated section), optional end_nanos (bounded
  interval), original_tz_offset_min + original_time_text preserved exactly
  as entered, archived, revision. Markers are always manual.
- `report_definitions` — title/subtitle, sections_json (ordered; narrative
  sections carry user-authored content or explicit `unknown`),
  selected_evidence_json (`[{evidence_id, revision}]` — exact revisions),
  selected_markers_json, redaction_profile_id + captured profile version,
  options_json (display tz, excerpt bounds, path policy), revision.
- `report_artifacts` — immutable generation records: format
  (`markdown|html`), destination, snapshot_json (the consistent
  investigation snapshot: entity revisions, resolver states at capture,
  language/envelope/schema/app versions, tz, profile version), SHA-256,
  byte_size, status (`completed|failed|cancelled`), created_at.
- `redaction_profiles` — name, version (bumps on rule change), rules_json
  (ordered typed rules), posture_json (path policy, bounds, allow/deny).
- `bundle_exports` / `bundle_imports` — immutable export records
  (manifest_json, SHA-256, status) and import provenance in the *destination*
  workspace (original path, bundle checksum, manifest, detail).
- `case_history` — the single non-destructive history/activity ledger:
  (investigation_id, entity_kind, entity_id, revision, action,
  payload_json = full entity state after the action, detail_json,
  created_at). Every material mutation inserts its history row in the same
  transaction. No OS identity is captured anywhere. Content hashes, when
  present, are labeled integrity aids — never signatures/authorship proof.

Rollback safety: the migration is a single transaction (rusqlite `tx`),
so a failure leaves schema v2 intact and reopenable by 0.2.0 builds.
Forward-only policy unchanged (`SchemaTooNew` guards downgrades). The
migration only CREATEs new tables — it rewrites no v2 data and touches no
Parquet, so a migrated-then-rolled-back workspace loses nothing.

Concurrency: every update statement is `... WHERE <id> = ? AND revision = ?`;
zero affected rows surfaces `case/stale-revision` (structured conflict), the
UI reloads and re-applies. Last-write-wins is never silent.

## Dataset revision fingerprint

v0.3 needs a durable "dataset revision" for evidence capture. Definition:
`dsrev-<hex>` = `stable_id` digest over the dataset's published segment set
(sorted `(segment_id, row_count, byte_size)` triples) + dataset_id +
storage schema version. Computed from `segments` on demand; captured into
evidence/scope refs at pin time. Properties: append-only imports change the
fingerprint (drift is visible), identical sets reproduce it byte-for-byte,
and it needs no new table. It is an integrity aid, not a signature.

## Evidence resolution and integrity-state decision table

One resolver (app layer), batched (single id-set lookup per dataset via the
existing temp-table pattern — no per-evidence queries), bounded, cancellable
via the existing cancel registry, and read-only: verification NEVER rewrites
`snapshot_json`, never re-imports, never mutates references. Updating a
reference is an explicit user action producing a new revision or a
superseding evidence item.

States (stored in `evidence.resolver_state`; details in
`resolver_detail_json`):

| State | Condition (first match wins per kind) |
|---|---|
| `unverified` | never verified, or verification cancelled before this item |
| `unsupported_reference_version` | envelope_version / query language_version newer than this build understands |
| `broken` | canonical reference unresolvable AND captured snapshot absent/undecodable |
| `dataset_revision_unavailable` | referenced dataset deleted, or recorded `dsrev` cannot be recomputed (segments gone) |
| `source_missing` | canonical record resolves; registered source file absent on disk |
| `source_changed` | source file present but BLAKE3/content identity ≠ captured fingerprint (size fast-path, hash confirm) |
| `canonical_available_source_unavailable` | canonical data resolves; original bytes cannot be checked (no registered path — e.g. bundle-imported subset, or managed copy absent) |
| `partially_resolved` | bounded selection: some IDs resolve, some do not (per-id detail recorded) |
| `query_drift` | query/group/interval evidence: query no longer validates (`validates:false` + diagnostics), or validates + runs with result metadata (count, omitted-untimestamped, resolution fingerprint) ≠ captured |
| `verified` | reference resolves and every expected fingerprint/revision matches |

Refinements fixed during W2 implementation (ADR-0017):
- reference undecodable at a **supported** version with readable snapshot
  → `unsupported_reference_version` (`cause:"undecodable"`); `broken`
  only when reference AND snapshot are both unreadable;
- record vanished from a still-present dataset →
  `dataset_revision_unavailable` + `record_found:false` (the captured
  revision no longer resolves);
- `source_missing`/`source_changed` apply generically per kind: for item
  evidence the item is the source (deleted → missing; revision advanced
  → changed with both revisions in detail).

Secondary conditions that do not change the primary state are recorded in
`resolver_detail_json` under `secondary` (dataset revision advanced while
the pinned record still verifies; profile/parser version drift;
saved-search renamed/deleted — reported, never substituted; non-exact
captured count marked `count_unchecked`). Distinct conditions are never
collapsed into a green/red boolean; the UI renders each state with text +
icon (never color alone). Hash scope is documented: source fingerprints
are full-file BLAKE3 (`source_files.content_hash`) with a size fast-path;
absence of a checkable hash is its own visible state, and nothing weaker
is presented as full verification.

## Report, redaction, and bundle schemas (contract summary)

- Report sections: `summary, impact, symptoms, timeline, hypotheses,
  evidence, root_cause, resolution, validation, follow_up` — narrative
  sections are user-authored on the report definition (empty / omitted /
  explicit `unknown` per documented rule; nothing synthesized); timeline /
  hypotheses / evidence sections are projections of selected data at exact
  captured revisions, keeping hypothesis states and integrity warnings.
- Markdown: deterministic UTF-8, LF line endings, no raw HTML passthrough
  (everything escaped/coded), dynamic fence sizing so embedded backticks
  cannot escape, stable `ev-` anchors, explicit truncation/approximation/
  missing-source labels, no absolute local paths by default.
- HTML: one self-contained file — embedded CSS only, zero scripts/forms/
  iframes/objects/remote anything, restrictive CSP meta
  (`default-src 'none'; style-src 'unsafe-inline'`), all content escaped by
  context, imported URLs/paths never become links, charts (if any) are
  static inline SVG/tables without handlers. Readable with JS disabled
  because no JS exists.
- Redaction = export projection shared by MD/HTML/bundle: rules (field
  omission, masking with fixed token, exact replacement, bounded safe-regex
  replacement, provenance path policy omit|basename|include, field
  allow/deny lists, excerpt/record bounds, optional deterministic
  pseudonymization labeled as such). Never mutates canonical data. Preview
  is generated by the SAME projection as final bytes and is invalidated by
  any input change. Heuristic secret detection, if any, is labeled
  best-effort warnings only.
- Bundle `.logscope-case` = standard ZIP: `manifest.json`,
  `investigation/*.json`, `evidence/evidence.jsonl`, `queries/queries.json`,
  `timeline/markers.json`, `data/logs.parquet` (declared closure subset),
  `excerpts/…` (explicit only), `reports/report.{md,html}`,
  `checksums.sha256`. Manifest: bundle schema version, min compatible
  capability contract, investigation identity + captured revision, entry
  list w/ type/size/SHA-256, evidence/query schema versions, dataset +
  reproduction-scope descriptors (`full_within_declared_dataset_revision |
  included_subset_only | snapshot_only`), disclosure summary, explicit
  inclusion decisions, truncation states, no absolute paths. Checksums =
  integrity detection, not authorship. Import: strict manifest validation,
  entry-path hardening (reject abs/traversal/drive/ADS/reserved/NUL/dup/
  case-collision/symlink/device), size/ratio/count/depth limits, checksum
  verification, staging + transactional import into a NEW isolated
  workspace, ID-conflict remap-or-refuse, provenance recorded, unsupported
  versions fail actionably. Bundled reports are never auto-opened.

## Disclosure/redaction threat model (summary)

Assets: log content (may contain credentials/PII), local paths/usernames,
dataset shape, investigation narrative. Adversary for outbound artifacts:
the *recipient* (curious or hostile) and any renderer they use; for import:
a hostile bundle author. Mitigations: default-closed inclusion (nothing
leaves without explicit selection), projection-equality between preview and
final bytes, no hidden payloads (tests parse final HTML/ZIP and assert
excluded values absent), inert artifacts (no scripts/links/active content),
hardened import validation (above), and honest labels for every omission/
truncation/pseudonymization. Out of scope (documented, not implied):
cryptographic authenticity, non-repudiation, chain of custody, protection
from a malicious local administrator, bundle encryption/signatures
(deferred by milestone).

## Performance / privacy / security / compatibility risks

| Risk | Mitigation |
|---|---|
| N+1 evidence resolution | one batched id-set lookup per dataset (temp-table pattern), measured in benchmarks |
| Evidence snapshots bloat webview | list virtualization; snapshots paged by evidence id; bounded snapshot sizes at pin time |
| Report/bundle materializes datasets | streamed writes through staging; closure planner enforces row/byte limits before writing |
| Hostile log text in reports | context escaping + fence hardening + HTML parser tests + zero-network test on opened artifact |
| Redaction bypass via metadata | single projection for preview+final; ZIP/HTML scanned for excluded values in tests |
| Hostile bundle import | validation-before-extraction, staging isolation, limits, checksum enforcement (WP7 test corpus) |
| Migration risk on real workspaces | additive-only 0003 in one transaction; v2 fixture round-trip test; recovery on interrupted migration |
| Concurrency conflicts | optimistic revisions + structured `case/stale-revision`; no silent last-write-wins |
| Windows-only leakage into shared core | `logscope-case` stays path-neutral (relative paths, forward-slash entry names in bundles) |

## Measured benchmark environment

Unchanged reference machine: i9-12900KF (24 threads), 64 GB RAM, NVMe,
Windows 11 Pro 10.0.26100, release profile. Corpora: deterministic
`logscope-testsupport` generators (seed 20260729), never committed. v0.3
additions: 1,000-evidence investigation fixture; 5M-record migrated
workspace; bundle round-trip corpus. Budgets: inherited ADR-0010
interactive ≤ 1 s / cancel ≤ 250 ms; new operations get provisional
evidence-backed budgets recorded in an ADR after first measurement (never
redefined after an unfavorable run).

## Validation commands and results (live log)

```
$env:Path = "$env:USERPROFILE\.cargo\bin;$env:Path"    # cargo not on PATH
cargo fmt --check                                       # PASS (2026-07-30)
cargo test --workspace                                  # PASS: 169 tests / 38 suites / 0 failures (2026-07-30)
# P1 smoke: dist-portable 0.2.0 logscope.exe alive 5 s, window "LogScope"

# W1 (investigation domain):
cargo test -p logscope-case -p logscope-workspace       # PASS: 3 + (2 db + 9 case_domain + 5 lifecycle)
#   case_domain proofs: lifecycle+history+reopen, stale-revision rollback,
#   FK rollback, hypothesis states/links, items all kinds + archive,
#   guarded reorder, scope refs, no-OS-identity, v2->v3 migration fixture
cargo fmt && cargo clippy --workspace --tests           # clean, zero warnings
cargo test --workspace                                  # PASS: 181 tests, 0 failures
#   (pre-existing pre_v02 migration test updated: it asserted the literal
#    schema version 2; intent is "migrates to the latest supported
#    schema", now asserted via db::supported_schema_version())

# W2 storage slice (envelope v1 + evidence repos):
cargo test -p logscope-case -p logscope-workspace       # PASS (envelope round-trip,
#   version refusal, selection/interval bounds, snapshot size bound;
#   pin/edit/group/archive/supersede; resolver-columns-only verification)
cargo test --workspace                                  # PASS: 188 tests, 0 failures (7c73a3d)

# W2 remainder (dsrev + pin services + resolver, logscope-app/src/case.rs):
cargo test -p logscope-app --test case_flow             # PASS: 12 tests
#   proofs: dsrev determinism; event pin captures provenance identity +
#   raw excerpt + catalog-resolved display fields; selection partial
#   resolution; query pin freezes RelativeToLatest to concrete bounds +
#   saved-search identity reported (changed/missing, never substituted);
#   group predicate via authoritative language incl. NOT field:* missing
#   group; interval half-open bounds; item revision drift; source
#   append/flip/delete walk → size/content_hash/missing; every remaining
#   state via crafted rows (unsupported envelope+language versions,
#   undecodable-vs-broken, dataset gone); 10 pins → dataset_lookups == 1
#   (no N+1); pre-cancelled run writes nothing; snapshots byte-identical
#   through every verification.
cargo fmt --check && cargo clippy --workspace --tests   # clean, zero warnings
cargo test --workspace                                  # PASS: 200 tests, 0 failures
```

Results are appended here as work packages complete.

## Work log — completed / pending / blocked / deferred

- Completed: preflight (matrix above); W0 plan (83c84c8); W1 investigation
  domain (21f9d21: migration 0003 → schema v3, `logscope-case` crate,
  `case_meta` repositories, ADR-0016, 12 new tests, sweep 181/0); W2
  storage slice (7c73a3d: envelope v1 typed references + bounded
  snapshots + version-gated decode in `logscope-case::envelope`,
  evidence/group repositories with pin/edit/group/archive/supersede and
  resolver-columns-only verification, sweep 188/0); W2 remainder (this
  commit: `logscope-app/src/case.rs` — `dataset_revision` dsrev helper,
  six pin services through the authoritative pipeline, batched
  cancellable `verify_evidence` resolver covering all ten states,
  ADR-0017, 12 case_flow tests, sweep 200/0).
- Completed (cont.): W3 investigation + evidence UX (branch
  `feature/v0.3-w3-case-ux`). Rust boundary `12b38d8`: 20 case DTOs in
  `dto.rs` (i64 fields annotated `#[ts(type = "number")]` — the Tauri
  boundary is JSON, so `bigint` declarations were a type-level fiction),
  27 commands in `case_cmds.rs` reusing the pooled engines and the
  export job pattern for verification (progress via `job-event`, cancel
  via the existing `cancel_job`, terminal `verify-finished` event);
  `evidence_restore_context` decodes envelopes Rust-side so the UI never
  parses one. React: `Case.tsx` (investigation list/create/edit/status,
  hypotheses with audited states, items with per-entity
  pending/saved/failed/conflicted indicators driven by
  `workspace/stale-revision`, keyboard-accessible reorder, activity
  feed), `EvidencePanel.tsx` (flat+grouped, all ten resolver states
  rendered distinctly, batch verify with progress+cancel, supersession
  + per-evidence history, snapshot display, hypothesis linking),
  `PinDialog.tsx` (shows exact kind/target/scope/bounds before
  pinning), Explorer wiring (pin query/selection/event/facet-group/
  histogram-interval/saved-search, checkbox multi-select, jump-back
  restore that freezes relative strategies to their pin-time bounds —
  never broadened). Sweep: fmt clean, clippy clean workspace-wide,
  **234 tests green**, `tsc && vite build` clean, desktop debug build
  links.
- Completed (cont.): W4 investigation timeline. Storage: marker
  repository in `case_meta` (create/update/archive/list with the same
  same-transaction history + revision-guard discipline; interval rule
  end-requires-start, end-after-start enforced; original timestamp text
  + zone offset preserved verbatim). Read model
  (`logscope-app/src/timeline.rs`): markers + evidence merged into one
  deterministic order — instant, then instant-before-interval at the
  same instant, then id; time derivation per evidence kind is documented
  per entry (`marker | event_time | interval_bounds | resolved_window |
  none`) via Rust-side envelope decode; the undated section is explicit
  and every undated entry states its reason (no event time, unbounded
  scope, manual item, unreadable envelope — a future-version envelope
  lands undated rather than erroring the view); archived entries are
  excluded and counted. `parse_marker_time` accepts RFC 3339 with any
  offset, normalizes to UTC nanos, preserves the offset in minutes.
  Boundary: 5 DTOs + 4 commands (`create_marker`, `update_marker`,
  `set_marker_archived`, `investigation_timeline`); marker kind
  validated against the vocab at the boundary; bundle now carries
  markers. React `Timeline.tsx`: merged chronology (UTC axis), original
  entered text shown beside normalized times, undated section with
  reasons, marker add/manage/archive. Sweep: fmt clean, clippy clean,
  **245 tests green** (11 new: marker CRUD/conflicts/history/reorder,
  merge determinism, every undated rule, archived exclusion, interval
  bounds, RFC 3339 parsing incl. refusal), `tsc && vite build` clean,
  desktop debug build links.
- Completed (cont.): W5 reports. Storage: `report_definitions`
  (revision-guarded + history) and `report_artifacts` — immutable
  two-phase records (`running` inserted before any byte, finished
  exactly once; a finished outcome can never be rewritten; a crash
  leaves the `running` tombstone as honest evidence). One self-deadlock
  found and fixed during this slice: repository methods must not
  re-enter `MetaDb::raw()` (non-reentrant mutex) — the new artifact
  fns query on the held connection. Generation
  (`logscope-app/src/report.rs`): snapshot at **exact selected
  revisions** — a drifted selection is recovered from the history
  ledger's full-state payloads and rendered with an explicit "as
  captured at revision N" label; unrecoverable selections state that
  instead (nothing silently substituted). Renderers: deterministic
  Markdown (UTF-8/LF, inline escaping, dynamic fences sized past any
  embedded backtick run, stable `ev-` tokens in headings, blank
  narrative renders the explicit word *unknown*) and self-contained
  HTML (CSP meta `default-src 'none'; style-src 'unsafe-inline'`, zero
  scripts/links/iframes, context escaping everywhere, paths/URLs render
  as text). No wall-clock time enters the document bytes — generation
  time lives on the artifact row, so identical inputs give identical
  bytes. The timeline section reuses the same entry derivation as the
  timeline view (`marker_entry`/`evidence_entry`/`order_entries`
  extracted for shared use — one derivation, no drift). Staged write
  (`.partial-<uuid>` + atomic rename, destination-exists refusal) with
  SHA-256 recorded on the artifact. Boundary: 6 DTOs + 5 commands;
  sections validated fail-fast at the boundary. React `Reports.tsx`:
  definition editor (canonical section order, narrative textareas with
  the unknown rule stated, exact-revision evidence/marker selection),
  generate to MD/HTML via save dialog, immutable artifact list with
  checksums. Sweep: fmt clean, clippy clean, **257 tests green** (12
  new incl. byte-determinism, hostile-content inertness in both
  formats, the unknown rule, drift labels, overwrite refusal,
  artifact-checksum equality), `tsc && vite build` clean, desktop
  links.
- Completed (cont.): W6 disclosure projection. Storage: redaction
  profiles with the semantic-version rule — `profile_version` bumps on
  any rule/posture change but not on a pure rename (asserted), so an
  artifact's named profile version pins exactly what shaped it;
  attach/detach on report definitions via FK-enforced
  `set_report_def_redaction`. Engine (`logscope-app/src/redact.rs`):
  ordered typed rules (omit_field / mask_field with the single fixed
  token / replace_exact / bounded linear-time replace_regex /
  deterministic pseudonymize labeled as such), posture (path policy
  default-**omit**, deny-wins allow/deny lists, per-block text bound),
  structural snapshot projection ({name,value} field entries matched by
  name, path keys under the path policy, every string leaf through the
  text rules; an unparseable snapshot is projected as opaque text so
  raw bytes cannot bypass rules). Hostile profiles are structured
  refusals: unknown rule kinds, unknown posture keys
  (`deny_unknown_fields`), >128 rules, >512-char or >1 MiB-compiled or
  invalid regexes. Report integration: the projection runs over every
  user-authored and captured string in both renderers; the artifact
  opens with a disclosure statement naming the profile + version and
  the honest application counts; `snapshot_meta` records the profile
  identity; an invalid attached profile refuses BOTH preview and
  generation (nothing unprojected is ever published under a broken
  profile). **Preview is the final bytes**: one shared render path
  (`render_snapshot`) serves `render_preview` and `generate_report`,
  and equality is asserted byte-for-byte in both formats. Proofs also
  assert: excluded values absent from the whole artifact (title,
  narrative, evidence, snapshot), canonical rows untouched after
  generation, no-profile reports render verbatim (redaction is an
  explicit choice), pseudonym determinism. Boundary: profile CRUD
  compile-validates before storing; `preview_report` command; React
  `RedactionPanel.tsx` (typed rule rows + posture editor) and Reports
  wiring (per-definition profile select, preview modal rendering the
  exact bytes as text). Sweep: fmt clean, clippy clean, **265 tests
  green** (8 new), `tsc && vite build` clean, desktop links.
- Completed (cont.): W7 portable case bundle. Export
  (`logscope-app/src/bundle.rs`): closure planner collects the record
  ids every reference names (event, selection, bounded
  representatives), bounded at 200k; the data subset is written by the
  engine (`COPY … read_parquet JOIN temp ids ORDER BY record_id`) into
  one `data/logs.parquet`. **Under a disclosure profile no raw data is
  exported at all** — the projection cannot yet guarantee parquet row
  content, so default-closed wins: scope `snapshot_only`, decision
  recorded in the manifest's explicit inclusions. Every outbound string
  (metadata, evidence envelopes INCLUDING references — they carry query
  text and source paths — markers, saved searches) passes through the
  same `Projection` reports use; the planted-secret proof asserts
  absence from every ZIP entry. Deterministic ZIP (fixed timestamps,
  stable order, ordered rows; byte-equality asserted). Exhaustive
  manifest (type/size/SHA-256 per entry, schema + min-compatible
  version, investigation identity + captured revision, disclosure note
  with counts, explicit inclusion decisions). Reports are explicit
  opt-in and re-checksummed at export — an altered artifact file
  REFUSES the bundle rather than shipping silently. Two-phase
  bundle_exports record.
  Import: validate-everything-before-extracting — path hardening
  (absolute, traversal, backslash, colon [drive/ADS], control chars,
  reserved device names, trailing dot/space, depth>8, case-collisions,
  symlink entries, directory entries), count/size limits, manifest
  gate (min-compatible version fails actionably: "update LogScope"),
  **exact entry-set equality** (hidden payloads and missing entries
  both refuse), per-entry declared-size + SHA-256 enforcement with an
  inflate-past-declared cutoff. Staged build of a NEW isolated
  workspace renamed into place only on success — a refused import
  leaves nothing (asserted per hostile case). IDs preserved for
  provenance; duplicates within a bundle refuse. `bundle_imports`
  records path/checksum/manifest/summary in the destination. Bundled
  reports land as inert files under `imported-reports/` and are never
  opened; the parquet subset lands under `imported-data/` and is **not
  registered as a queryable dataset** (documented residual — the
  investigation stays readable through evidence snapshots, and
  verification honestly reports `dataset_revision_unavailable`).
  Boundary: export command on the engine pool + import command +
  export list; React: bundle export row in Reports (profile select +
  include-reports + save dialog), home-screen "Import case bundle…"
  flow that opens the new workspace and states the inert-report rule.
  Sweep: fmt clean, clippy clean, **273 tests green** (6 new proofs
  incl. full import→pin→export→import round-trip with real parquet),
  `tsc && vite build` clean, desktop links.
- Pending: W8–W9, W-R (table above).
- Blocked: none.

- Completed (cont.): W8 reliability. Crash recovery: workspace open's
  recovery sweep gained step 5 — `running` report-artifact and
  bundle-export records are finished as failed (`job/interrupted`),
  tombstones completed, never deleted; `RecoveryReport`/`RecoveryDto`
  extended (`interrupted_case_records`); proven by test including
  idempotency (a second reopen recovers nothing). Benchmark: new
  `bench_case` bin; measured 5M/1,000-evidence numbers recorded in
  `docs/benchmarks/2026-08-05-v0.3-case-baseline.md` — import 222.9 s
  (22.4k rec/s), pin 103.5 ms each (interactive number, not a bulk
  API), **verify 1,001 evidence in 1.10 s with `dataset_lookups = 1`
  asserted in the bench binary** (no-N+1 at scale), timeline 4 ms,
  report 62 ms, bundle export 2.9 s incl. parquet subset over 5M,
  peak WS 1.73 GiB. Cancellation latency measured honestly: p50 684 ms
  cancel→return, phase-bounded (the in-flight batched engine query
  completes before the next checkpoint; engine-level interrupt is not
  wired into verify's internal queries — recorded as a known property
  with the fix named if sub-100 ms is ever required). Accidental
  honesty proof kept in the doc: bench event pins verify as
  `source_missing` because the temp corpus is deleted after import —
  the resolver reports the truth rather than `verified`.
- Pending: W9, W-R (table above).
- Blocked: none.

- Completed (cont.): W9 test sweep — suite now **292 green** (274 + 18).
  New proofs: `envelope_corpus.rs` (logscope-case; seeded-PRNG mutation
  + arbitrary-JSON corpora — decode never panics, returns only
  `DecodeOutcome`, deep nesting is `Undecodable` not a stack overflow,
  `bound_field` cuts at char boundaries); `corpus_hardening.rs`
  (entry-path validator proven **equivalent to an independent invariant
  oracle** over 8k generated names + hostile table; marker-time corpus
  with exact instants/offsets; hostile redaction profiles; generated
  allowlist-leak property); `e2e_workflow.rs` (one chain: import →
  investigate → pin ×11 → verify `dataset_lookups=1` all `verified` →
  timeline (11 dated / 1 undated with reason) → report preview==bytes,
  regeneration byte-identical → bundle export byte-identical →
  re-import: links/state/marker-original-text survive, provenance
  checksum recorded, imported verification reports
  `dataset_revision_unavailable` ×11 and never `verified`);
  `error_codes.rs` (**the registry**: 120 stable codes, sorted; a
  source scan over every crate + Tauri boundary asserts observed ==
  registered both ways; ~30 codes produced through their real paths,
  incl. schema-too-new via a future migration row and query/cancelled
  as the F-1 pre-execution-cancel regression pin).
  The sweep caught two real defects, both fixed here:
  1. `redact.rs` — serde's derived struct deserializer accepts JSON
     *arrays* positionally, so posture `["include"]` would have widened
     `path_policy` silently; posture now must be a JSON object.
  2. `bundle.rs` — hypothesis→evidence links and hypothesis state were
     dropped by export/import (W7's round-trip test never linked any).
     Export now embeds `linked_evidence_ids` per hypothesis (added
     after projection; ids are opaque); import restores links after
     evidence insertion (link to absent evidence = hostile refusal) and
     restores a cleanly-parsing state, leaving `unverified` otherwise —
     the import never upgrades a claim it cannot read. Older bundles
     (no key) import unchanged; no schema-version bump needed.
  Corpus documentation catch: chrono's RFC 3339 parser accepts a space
  separator (`2026-08-04 10:00:00Z`) — recorded in the valid corpus.
  Report definitions do NOT travel in bundles (artifacts only, inert) —
  asserted as the documented residual.
- Pending: W-R (table above).
- Blocked: none.

Continuation (fresh session): read this file first. Next task — **W-R
release prep**: `tools/logscope-setup` byte-equivalence (gate-19 debt),
docs + ADR sweep (new ADR needed? posture-object rule and bundle link
restore are contract clarifications — record in ADR-0018 family or the
work log), version bump to 0.3.0 everywhere (workspace Cargo.toml,
tauri.conf.json, package.json), both artifacts via
`scripts/package-portable.ps1`, final 32-gate acceptance matrix —
`BLOCKED` ≠ `PASS`, and the standing residual stays recorded: no UI
automation, human walkthrough of setup.exe + case UI is an owner
action.
1. Tauri boundary: extend `logscope-app/src/dto.rs` (ts-rs DTOs mirroring
   `case_meta` rows + `case.rs` pin requests/report) and add typed
   commands in `apps/desktop/src-tauri` following the existing explorer
   command pattern (structured JobError envelopes, no storage access from
   React).
2. React: investigation workspace view (create/select/edit/archive/
   restore; status/severity/owner-text/tags/window editing; hypotheses
   with states + linked evidence; notes/tasks/findings/questions with
   pending/saved/failed/conflicted indicators from optimistic-revision
   errors), Explorer pin actions (row/detail/selection/query/saved/
   facet-group/histogram-interval) with a pin dialog exposing exact
   target/kind/scope/counts/truncation/snapshot-limit/annotation/
   relevance, and the evidence panel (list + grouped, integrity states
   text+icon, verify batch with progress+cancel via the jobs channel,
   supersede/history display, jump-to-Explorer restoring the saved
   context verbatim — never broadened).
3. Reuse `case.rs` services only; no new query paths. Keyboard access +
   labels per the milestone accessibility list.
Validation: `cargo fmt --check && cargo clippy --workspace --tests &&
cargo test --workspace && npm --prefix apps/desktop run build` (plus
existing UI checks).
- Deferred (milestone boundaries): pattern mining/clustering/anomaly/
  correlation/auto-findings (v0.4), CLI/Agent API/MCP/plugins, metrics/
  traces surfaces, live tailing/OTLP productization, cloud/team features,
  bundle encryption/signatures, arbitrary SQL.

## v0.3 acceptance matrix (final — to be completed)

Populated at milestone end against the 32-gate list; `BLOCKED` ≠ `PASS`.
